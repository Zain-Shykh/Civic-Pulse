"""RuleBasedTriage — deterministic keyword fallback. Always available, never fails.

This is the fallback target for LLMTriage's retry/timeout exhaustion path
(triaged_by = "rules:fallback") and a standalone selectable provider
(triaged_by = "rules"). Stub only for this phase.
"""

pass
