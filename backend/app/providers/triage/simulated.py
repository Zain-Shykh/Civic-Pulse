"""SimulatedTriage — deterministic fake for CI. Seeded, no network, configurable failure injection.

TRIAGE_PROVIDER=simulated is what CI pins to (docs/CONTRACTS.md,
"Determinism, and how to test a system that is not"), so the test suite
stays green regardless of live-provider flakiness. Stub only for this phase.
"""

pass
