apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# Locally-built dev tags (docs/specs/phase-11-kubernetes-manifests.md, Open
# Question 2) — same names compose.yaml already builds. Import into the k3d
# cluster with `k3d image import` before applying (see README.md).
images:
  - name: civicpulse-backend
    newTag: dev
  - name: civicpulse-frontend
    newTag: dev
